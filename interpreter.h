
#ifndef INTERPRETER_H
#define INTERPRETER_H
#include "pcb.h"

int interpreter(char* command_args[], int args_size);
int help();
// int create_pcbs(char *commands[], int num_commands, struct PCBStruct pcbs[]);
// int set_pages(struct PCBStruct pcbs[], int num_pcbs);
// int add_pcbs(struct PCBStruct pcbs[], int num_pcbs);

#endif
